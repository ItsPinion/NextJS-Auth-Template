export default function Logo({
  className,
  
}: {
  className?: string;
  textColor?: string;
}) {
  return <h1 className={className + " text-4xl font-bold text-white font-serif"}>;.; Logo</h1>; 
}
